function Button({onClick}) {
    return (
        <div>
             <button onClcik = {onClick}
              className='btn'
              >Add
              </button>
        </div>
    )
}

export default Button
